# 金融投研 Agent 项目迁移交接文档

> 更新时间：2026-08-06（Asia/Shanghai）
> 工作区：`/Users/dongyushu/Downloads/vibe- coding/投研agent`
> 交接目标：让新的开发 Agent 不依赖原对话也能理解产品边界、架构、当前代码、Live 配置、验证证据和剩余风险。

---

## 1. 一页结论

这是一个面向中国 A 股个股研究的本地单机 Web MVP，技术栈为 FastAPI + SQLite + LangGraph + Pi Agent Runtime + 原生 HTML/CSS/JavaScript。它已实现两条独立研究流程：

1. `technical_v1`：证券解析、行情和指标、Technical Research Agent、Kronos 时序模型、Technical Assembly Agent、技术面报告。
2. `fundamental_v1`：Lead、Business、Industry、Financial、Valuation、Final Review、Writer、Evidence/Assumption、结果版本/失效、基本面正式报告。

当前源码级别可称为 **MVP Code Complete**，且最新本机 Live 组件验证已覆盖：

- Pi SDK 原生 DeepSeek provider 的本地 provider/model 预检；
- AKShare 真实行情、公司资料和 5 期年度财务数据；
- 官方 Kronos-mini + Tokenizer-2k 的 Apple MPS 真实推理；
- 巨潮资讯公告检索和 PDF 文本提取。

但是仍不应宣布为完整 **MVP Live Ready**，原因是：

- 用户尚未在本机配置真实 `DEEPSEEK_API_KEY`，因此没有进行真实 DeepSeek 请求或两条完整 Live Agent Run；
- Tavily 只保留 adapter，按用户要求暂不配置；
- Docker 尚未实机验证，而且当前 Dockerfile 尚未包含 Kronos Live 所需的依赖和官方源码；
- GitHub 远端与本地实现严重不同步，详见下文。

---

## 2. 最高优先级迁移风险：Git 还没有保存当前成果

### 2.1 当前 Git 状态

- 仓库：`https://github.com/28889-stack/investment-research-agent.git`
- 分支：`main`
- 本地 HEAD / 远端 HEAD：`fe316f3 chore: normalize file endings`
- 远端只有两个初始提交：
  - `615a4d5 chore: initialize repository`
  - `fe316f3 chore: normalize file endings`
- `.gitignore` 和 `README.md` 有未提交修改。
- `app/`、`tests/`、`pi_bridge/`、Docker/ops 文件、产品/技术方案等绝大多数项目文件仍是 **untracked**。

### 2.2 对新 Agent 的明确警告

**不要从 GitHub 重新 clone 后就认为获得了当前项目。** 当前真实成果主要在这个本地工作区。

迁移前应由用户确认后执行：

1. 检查全部 untracked 文件，排除运行数据、密钥、缓存和不应提交的个人文件。
2. 运行完整测试和 `git diff --check`。
3. 创建正式基线提交并 push。
4. 再在新环境 clone 验证。

本文档不代替 Git 迁移。

---

## 3. 产品定位和不可突破的边界

### 3.1 已确定的产品目标

- 用户输入股票名称或代码，选择技术面或基本面分析。
- API 异步创建任务，独立 Worker 执行，前端轮询进度。
- 数据计算、证据检索、LLM 推理和报告写作分离。
- 关键数字由确定性 Python/JSON Artifact 提供，LLM 只负责解释和组织。
- 基本面结论通过 Evidence 和 Assumption ID 追溯。

### 3.2 MVP 明确不做

- 不做自动交易。
- 不输出直接买卖指令、确定性收益承诺或综合投资评级。
- 不生成技术面 + 基本面综合评分/合并报告。
- 不支持自动多轮 Writer 返工。
- 不做回测、多市场统一、云端多租户、分布式任务。

### 3.3 相关权威文档

- `金融投研Agent产品方案_V2.1.md`：产品目标、Agent 分工、Schema、状态机和 MVP 范围。
- `金融投研 Agent 技术方案.md`：当前单机架构和六阶段实施方案。
- `README.md`：最接近当前代码的使用和实现说明。
- `MVP_ACCEPTANCE.md` / `RELEASE_CHECKLIST.md`：第六阶段当时的验收快照，**Live 部分已被后续工作部分超越，不是最新事实**。

---

## 4. 当前技术架构

```text
原生 Web 前端
  ↓ HTTP/JSON
FastAPI Web/API 进程
  ↓ SQLite 任务队列
单 Research Worker
  ├─ technical_v1 LangGraph
  └─ fundamental_v1 LangGraph
       ↓
PiAgentAdapter / ToolRegistry / ContextLoader / OutputValidator
       ↓ JSONL stdin/stdout
Node Pi Bridge
  ├─ MockPiClient
  └─ Pi Agent Core + Pi AI provider SDK

持久化：
  data/research.db      业务任务、事件、Agent/Tool 执行记录
  data/checkpoints.db   LangGraph Checkpoint
  data/artifacts/       任务级权威数据和报告
```
### 4.1 主要技术栈

| 层 | 技术 |
| --- | --- |
| Web/API | Python 3.13.9、FastAPI 0.141.1、Uvicorn 0.52.1 |
| 业务存储 | SQLite + SQLAlchemy 2.0.51 |
| 工作流 | LangGraph 1.2.10 + `langgraph-checkpoint-sqlite` |
| Agent Bridge | Node.js 24.12.0（最低要求 22.19）+ TypeScript |
| Pi SDK | `@earendil-works/pi-agent-core` 0.83.0 + `@earendil-works/pi-ai` 0.83.0 |
| LLM | Pi 原生 `deepseek` provider，目标模型 `deepseek-v4-flash` |
| 数据 | AKShare 1.18.81、pandas、NumPy |
| 技术图 | matplotlib，不依赖 TA-Lib |
| 时序模型 | PyTorch 2.13.0 + 官方 Kronos-mini |
| 来源读取 | httpx + pypdf + 定制 HTML 文本提取 |
| 前端 | 原生 HTML/CSS/JavaScript |
| 测试 | pytest 9.1.1 + Node `node:test` |

### 4.2 核心目录

| 路径 | 职责 |
| --- | --- |
| `app/main.py` | FastAPI 应用、API、页面入口、报告读取 |
| `app/worker.py` | 单 Worker 领取、恢复、分流和优雅停机 |
| `app/run_service.py` | Run 事务、状态转移、队列、取消、事件、报告读取 |
| `app/models.py` | `ResearchRun`、`RunEvent`、`AgentExecution`、`ToolExecution` ORM |
| `app/runtime/` | Pi Adapter、Bridge Client、Profile、Context、Schema 验证、Tool Registry、安全 |
| `app/technical/` | 行情、指标、Kronos、技术工作流和报告 |
| `app/fundamental/` | 基本面数据、证据、财务、估值、Writer、Manifest、报告 |
| `app/retrieval/` | 检索协议、封闭 provider registry、巨潮适配器 |
| `app/profiles/` | 10 个 AgentProfile JSON |
| `app/static/` | 首页、任务进度页、报告页 |
| `pi_bridge/` | Node/TypeScript Pi JSONL Bridge |
| `scripts/` | Live smoke、备份/恢复检查、Kronos 安装、巨潮爬虫 CLI |
| `tests/` | API、工作流、安全、恢复、Live 可选测试 |

---

## 5. 任务、状态和存储

### 5.1 ResearchRun

`ResearchRun` 保存：

- `run_id`、原始/标准化/解析后证券代码、证券名称；
- `analysis_type` (`technical` / `fundamental`)；
- `policy_id`、`as_of`、workflow 名称与版本；
- `status`、`stage`、`progress`、`current_node`；
- `cancel_requested`、错误、报告路径、`data_version`；
- 创建/开始/更新/完成时间和乐观锁版本。

### 5.2 终态

```text
COMPLETED
HUMAN_REVIEW_REQUIRED
FAILED
CANCELLED
```

`HUMAN_REVIEW_REQUIRED` 是有意设计的业务结果，不等于系统错误。当 Final Review 或 Writer 认为证据不足时，保留研究工作包供人工处理，不生成伪正式报告。

### 5.3 队列与 Worker

- `POST /api/runs` 写入 `CREATED`。
- 单 Worker 先恢复非终态任务，再按创建顺序领取新任务。
- `MAX_PENDING_RUNS` 默认 20，超限 API 返回 429。
- 跨进程 `fcntl` 维护锁防止多 Worker 重复执行，并将备份与正在运行的任务互斥。
- SIGTERM/SIGINT 会请求取消当前任务；取消在节点边界生效，不强杀正在运行的原生模型调用。

### 5.4 Checkpoint 和恢复

- LangGraph Checkpoint 使用 `data/checkpoints.db`。
- `thread_id` 固定为 `run_id`，作为任务隔离边界。
- Worker 重启后从未完成节点恢复。
- 节点恢复前会重新验证已有 Artifact，不会盲信 Checkpoint。
- 基本面 Manifest 发现上游 SHA 变化时，标记相关下游 `stale`，从最早受影响节点重建。

---

## 6. Pi Agent Runtime 的真实接入方式

### 6.1 不是重写 Pi，也不是简单 HTTP 直调

Python 工作流不直接实现 Agent Loop，而是通过 `PiAgentAdapter` 向独立 Node Bridge 发送 JSONL 协议。Node 侧使用：

- `@earendil-works/pi-agent-core` 的 `Agent`；
- `@earendil-works/pi-ai` 的模型目录和 provider；
- Python ToolRegistry 作为真正的工具执行边界。

Bridge 命令：

```text
health_check
validate_model
create_session
run_agent
close_session
shutdown
```

Agent 如果调工具，Node Bridge 通过 JSONL 回传 tool call，Python 根据 Profile 和 Pydantic Schema 完成授权、执行和记录，再把结果返回 Pi Session。

### 6.2 DeepSeek 配置

已实现：

```text
PI_RUNTIME_MODE=live
PI_MODEL_PROVIDER=deepseek
PI_MODEL=deepseek-v4-flash
PI_API_KEY_ENV_NAME=DEEPSEEK_API_KEY
```

Node Bridge 显式导入 `@earendil-works/pi-ai/providers/deepseek`，调用 `deepseekProvider()`。Python 子进程环境是限制的，只传递 PATH、LANG、运行模式和必需的 API Key；自定义密钥名会映射为 SDK 约定的 `DEEPSEEK_API_KEY`，密钥不进入 JSONL。

已验证 `deepseek/deepseek-v4-flash` 能被本地 SDK 目录解析，但因未配置真实 key，没有调用 DeepSeek 网络 API。

### 6.3 Full 与 Constrained Profile

| Profile | 模式 | 工具/限制 |
| --- | --- | --- |
| `technical_research` | Full | 3 个技术工具，最多 5 轮/5 次工具 |
| `technical_assembly` | Constrained | 1 轮，0 工具 |
| `fundamental_lead` | Full | 公司、检索、来源读取 |
| `business_research` | Full | 公司、检索、来源读取 |
| `industry_research` | Full | 检索、来源读取 |
| `financial_research` | Constrained | 1 轮，0 工具，读受信财务上下文 |
| `valuation_research` | Constrained | 1 轮，0 工具，读受信估值上下文 |
| `fundamental_writer` | Constrained | 1 轮，0 工具，只读已验收结果 |

另有 `full_runtime_smoke` 和 `constrained_runtime_smoke` 用于 Runtime 验证，共 10 个 Profiles。

### 6.4 输出验证

- Agent 输出必须为 JSON 对象，可从纯 JSON、JSON 代码块或包含文本的第一个合法 JSON 对象中取出。
- 根据 Profile 对应 Pydantic Schema 严格校验，多余字段禁止。
- 输出长度默认上限 20,000 字符。
- 最多修复 1 次；修复仍失败则节点失败。
- 基本面输出显式拒绝买卖评级、交易指令和确定性收益承诺。

---

## 7. technical_v1 详细流程

```text
resolve_security
  → technical_research
  → kronos
  → technical_assembly
  → write_report
```

### 7.1 节点职责

1. `resolve_security`：把 `600519`、`600519.SH`、`SH600519` 或精确中文名称解析为标准 `600519.SH`。
2. `technical_research`：Full Agent 只能调用：
   - `get_market_data`
   - `calculate_technical_indicators`
   - `get_technical_summary`
3. `kronos`：独立 Python 确定性节点，不使用 Pi，不读 Agent 文字，只读同一 `data_version` 的标准行情。
4. `technical_assembly`：Constrained Agent，0 工具；比较指标解释与 Kronos 预测，整理冲突、风险和不确定性。
5. `write_report`：Python 从权威 JSON 注入精确数字，生成 Markdown 和技术图。

### 7.2 行情适配

- Mock：与证券代码无关的确定性数据。
- Live provider：`akshare`。
- 主路径：东方财富 `stock_zh_a_hist`。
- 回退路径：新浪 `stock_zh_a_daily`。这是最新增加的真实数据容错，两条都失败时才报错，不回退 Mock。
- 标准列：`date, open, high, low, close, volume, amount`。
- 验证：至少 120 根、日期升序不重复、无非有限数、OHLC 合法、不越过 `as_of`。
- `data_version = {symbol}_{as_of}_{market_data.csv SHA256 前 8 位}`。

### 7.3 技术指标

由 pandas/NumPy 确定性计算，当前包括：收盘价、MA20/60/120、EMA12/26、MACD/DIF/DEA、RSI14、布林带、ATR14、20 日波动率、成交量比、支撑/阻力和有限形态标签。

### 7.4 技术产物

```text
market_data.csv
technical_indicators.json
technical_research.json
kronos_result.json
technical_assembly.json
technical_chart.png
technical_report.md
```

---

## 8. Kronos Live 详情

### 8.1 版本与本地位置

- 官方仓库：`https://github.com/shiyu-coder/Kronos.git`
- 锁定提交：`67b630e67f6a18c9e9be918d9b4337c960db1e9a`
- 源码位置：`vendor/kronos`（被主仓库 `.gitignore` 忽略）
- 锁文件：`config/kronos-source.json`
- 模型：`NeoQuasar/Kronos-mini`
- Tokenizer：`NeoQuasar/Kronos-Tokenizer-2k`
- 权重缓存：
  - `~/.cache/huggingface/hub/models--NeoQuasar--Kronos-mini`
  - `~/.cache/huggingface/hub/models--NeoQuasar--Kronos-Tokenizer-2k`
- 安装脚本：`python scripts/setup_kronos.py`
- 额外依赖：`requirements-kronos.txt`

### 8.2 运行策略

- mini 模型使用 2k tokenizer 和 `max_context=2048`。
- `KRONOS_DEVICE=mps` 时检测 PyTorch MPS；不可用则回退 CPU。
- predictor 按 `model_name + runtime_device` 缓存。
- 原生调用超时无法安全强杀 CPython 线程，所以 Live 推理使用 daemon 单飞线程：超时返回错误，但后台原生调用可能仍在结束。
- 同时只允许一个 Live Kronos 推理。
- Live 失败不降级到 Mock。

### 8.3 已验证的真实结果

- PyTorch：2.13.0；
- MPS：本机 `available=True`；
- predictor：`KronosPredictor`，device `mps`，context 2048；
- 用 600519.SH 行情做 2 交易日预测成功；
- 方向概率之和为 1.0，`model_version=NeoQuasar/Kronos-mini`。

---

## 9. fundamental_v1 详细流程

```text
resolve_security
  → lead_planning
  → business_research
  → industry_research
  → lead_review
  → financial_research
  → valuation_research
  → lead_final_review
  → fundamental_writer
  → write_fundamental_report
```

### 9.1 分工

- Lead Planning：生成研究主线和专业任务。
- Business：公司业务、商业模式、竞争优势和经营风险。
- Industry：行业结构、周期、竞争格局和政策/供需。
- Lead Review：检查前两个研究任务的完整性、冲突和缺口。
- Financial：Python 拉取三表并计算权威指标，Constrained Agent 只解释并提出结构化假设。
- Valuation：Python 计算 PE/PB/PS 和简化 DCF，Constrained Agent 解释敏感性和限制。
- Final Review：决定 `ready_for_writer`，列出 `missing_information`。
- Writer：只读已验收结果，不能搜索、建 Evidence/假设或改权威数字。
- Python Report：将 Writer 文字与权威 JSON 的精确数字组合成正式 Markdown。

### 9.2 基本面 Live 数据

- provider：AKShare。
- 公司资料：`stock_profile_cninfo`。
- 年度三表：东方财富年度资产负债表、利润表和现金流量表。
- 只保留 `as_of` 前已公告的共同期间，当前要求至少 5 期。
- 缺失数值保存 `null`，不伪造为 0；币种、单位和来源显式保存。
- 财务指标包括增长、利润率、ROA/ROE、负债/流动性、现金流质量、FCF、营运比例和周转率。
- 估值包括 PE/PB/PS 和 5 年简化 DCF，DCF 使用 Agent 生成但经 Schema 验证和落盘的增长率/永续增长/折现率假设。

### 9.3 Evidence 和 Assumption

- Evidence 保存于 `evidence.json`，ID 格式 `ev_NNN`。
- Assumption 保存于 `assumptions.json`，ID 格式 `asm_NNN`。
- Agent 只能引用已存在 ID；报告生成前再验证。
- 来源全文不交给 Writer；通过 ContextLoader 给出有界、标记为不可信数据的摘录。
- 正式报告只展开实际被引用 Evidence 的元数据和限长摘要。

### 9.4 结果 Manifest

`result_manifest.json` 保存每个基本面结果的：

- 当前版本；
- `current / stale / failed`；
- 输出文件 SHA256；
- 固定上游输入 SHA；
- 更新时间。

成功重建后版本 +1，但不保留旧文件副本。`stale` 报告不由报告 API 当作当前正式结果返回。

### 9.5 基本面产物（17 个）

```text
company_profile.json
evidence.json
assumptions.json
lead_plan.json
business_research.json
industry_research.json
lead_review.json
financial_data.json
financial_metrics.json
financial_research.json
valuation_result.json
valuation_research.json
lead_final_review.json
fundamental_research_package.md
fundamental_writer.json
fundamental_report.md
result_manifest.json
```

---

## 10. 检索/爬虫路径

### 10.1 当前默认 Live 方案

```text
RESEARCH_SEARCH_MODE=live
RESEARCH_SEARCH_PROVIDER=official_crawler
```

`official_crawler` 当前是巨潮资讯 A 股公告 adapter：

1. 从巨潮公开证券列表将代码映射为 `orgId`。
2. 调用巨潮公告查询端点。
3. 只接受 `finalpage/` 附件，生成 `https://static.cninfo.com.cn/finalpage/...` PDF URL。
4. 上海/深圳查询参数根据证券代码确定。
5. 公告毫秒时间戳按 UTC+8 转换为本地披露日期。

可独立运行：

```bash
python scripts/research_crawler.py 600519.SH 年度报告 --max-results 5
```

### 10.2 安全边界

- 通用 URL 读取只允许 HTTP/HTTPS 标准端口，拒绝凭证、localhost、内网/非公网 DNS 地址和自动重定向。
- DNS 解析后用第一个公网 IP 固定请求，并验证真实连接对端是公网地址。
- 本机代理使用 `198.18.x.x` Fake-IP，通用 SSRF 校验会正确拒绝该保留段。
- 为了读取巨潮 PDF，仅对精确白名单 `https://static.cninfo.com.cn:443/finalpage/` 使用系统代理兼容通道，其他 URL 不放宽。
- HTML 下载上限是 `research_max_source_chars * 8`字节；PDF 独立默认上限 20 MB；提取文本再按字符上限截断。
- PDF 仅支持可直接提取文字，不做 OCR。

### 10.3 Tavily 和 MCP

- Tavily adapter 仍在 `app/fundamental/evidence.py` 中，但用户要求暂不配置 API Key。
- `app/retrieval/protocol.py` 提供稳定 `ResearchSearchProvider` Protocol。
- `app/retrieval/registry.py` 是封闭 registry，配置不能动态 import 任意代码。
- 目前没有生产 MCP Server/Client；后续如需 MCP，应在 Protocol 后新增 adapter，不要让 Agent 获得任意网络工具。

---

## 11. API 和前端

### 11.1 API

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| GET | `/api/health` | 进程健康 |
| GET | `/api/readiness` | DB、Checkpoint、Artifact、Bridge、Profile、Workflow 就绪度 |
| GET | `/api/runtime/health` | Runtime/Profile/Tool/Checkpoint 摘要 |
| POST | `/api/runs` | 创建技术面或基本面任务 |
| GET | `/api/runs` | 最近任务，最多 50 |
| GET | `/api/runs/{run_id}` | 任务状态、进度、事件、usage |
| GET | `/api/runs/{run_id}/executions` | 安全的 Agent 执行摘要，不暴露思维链 |
| POST | `/api/runs/{run_id}/cancel` | 请求取消 |
| GET | `/api/runs/{run_id}/report` | Markdown、Bleach 清洗 HTML、版本和引用元数据 |
| GET | `/api/runs/{run_id}/artifacts/technical_chart.png` | 仅允许当前 run 固定技术图路径 |

### 11.2 前端

- `/`：输入证券、选择分析类型、创建任务。
- `/runs/{run_id}`：轮询任务进度、事件和 Agent 执行摘要。
- `/runs/{run_id}/report`：展示安全 HTML 报告、图表和引用/版本信息。
- 无 React/Vue 构建链，前端资源由 FastAPI 直接提供。

---

## 12. 配置、密钥和启动

### 12.1 模板

- `.env.example`：全 Mock 开发模板。
- `.env.live.example`：DeepSeek + AKShare + Kronos-mini + official crawler 的单机 Live 模板。
- `.env` 当前 **不存在**，且被 Git 忽略。
- 模板中 `DEEPSEEK_API_KEY=` 为空，并无任何真实密钥落盘。

### 12.2 新 Agent 本机启动步骤

Mock：

```bash
cd "/Users/dongyushu/Downloads/vibe- coding/投研agent"
source .venv/bin/activate
cp .env.example .env
npm --prefix pi_bridge install
npm --prefix pi_bridge run build
python -m app.main
# 另一终端
python -m app.worker
```

Live：

```bash
source .venv/bin/activate
python scripts/setup_kronos.py
npm --prefix pi_bridge run build
cp .env.live.example .env
# 在本机 .env 填入 DEEPSEEK_API_KEY，不要在聊天或 Git 中粘贴
python -m app.ops doctor
python -m app.main
# 另一终端
python -m app.worker
```

### 12.3 配置约束

- `development` 要求所有研究组件为 Mock。
- `staging` 要求所有研究组件为 Live。
- `production` 允许混合，用于单机渐进式配置。
- 默认仅绑定 `127.0.0.1`；绑定 `0.0.0.0`/IPv6 必须 `ALLOW_PUBLIC_BIND=true`。
- 当前没有身份认证，不应直接暴露到公网。
- `STORAGE_ROOT` 是持久化白名单根，DB/Checkpoint/Artifact/日志/备份路径必须在其下且互不嵌套。

---

## 13. 安全设计

- API Key 只使用环境变量，不写 SQLite/Artifact/日志。
- Bridge 标准输出只允许 JSONL 协议，stderr 经脱敏后记录。
- `api_key/token/authorization/password/secret/cookie`、Bearer 值和 URL 密码统一脱敏。
- ToolRegistry 在执行前同时校验 Profile 白名单、输入 Schema、运行上下文和超时。
- Agent 无 shell、任意 HTTP、任意文件、SQL 或 Python eval 工具。
- 网页/PDF 按 prompt-injection 风险处理，只作为不可信数据摘录。
- Markdown 转 HTML 后使用 Bleach 清洗。
- Artifact 读取使用 run 级固定路径，API 不接受任意文件路径。
- 不展示 Agent 内部推理过程，只展示结构化执行摘要。

---

## 14. 运维、备份和 Docker

### 14.1 Doctor

```bash
python -m app.ops doctor
```

检查 Python/Node 版本、Bridge build、启动配置、SQLite integrity/表结构、Artifact 可写、Profiles、Tools、两条工作流和 Live provider/model 本地预检。

### 14.2 备份和恢复检查

```bash
backup_path=$(python -m app.ops backup)
python -m app.ops restore-check --backup "$backup_path"
```

- 备份使用 SQLite Backup API，复制 `research.db`、`checkpoints.db` 和 Artifacts。
- 不备份 `.env`、日志、venv、node_modules 或模型权重。
- `restore-check` 在临时目录中检查 SHA/大小、SQLite integrity/必需表、Artifact JSON 和报告引用，不覆盖当前数据。

### 14.3 Docker 现状（重要）

Compose 定义了单 Web + 单 Worker、named volumes、本机 127.0.0.1 端口映射、非 root 用户和 360 秒停机宽限。

但当前 Dockerfile：

- 只安装 `requirements.txt`，没有安装 `requirements-kronos.txt`；
- 没有复制 `vendor/kronos` 或在构建/启动时执行 `setup_kronos.py`；
- 没有复制 `config/kronos-source.json`；
- 当前机器没有 Docker CLI，从未完成 `docker compose build/up` 实验。

因此：**Mock 容器方案接近完成，Kronos Live 容器方案尚未完成。**

---

## 15. 测试和最新验证证据

### 15.1 最新全量回归

2026-08-06 本轮最后验证：

```text
pytest -q
→ 262 passed, 5 skipped

cd pi_bridge && npm test
→ 10 passed, 0 failed
```

### 15.2 Live Doctor

使用 `.env.live.example` 和仅供本地预检的测试占位 key（未发送模型请求）：

```text
Python version             OK
Node version               OK
Pi Bridge build            OK
startup configuration      production / OK
live Pi model preflight    OK
research/checkpoint DB      OK
artifact directory         OK
profiles                   10 / OK
tool registry              11 / OK
technical workflow         OK
fundamental workflow       OK
live market_data           configured
live kronos                configured
live fundamental_data      configured
live research_search       configured
```

不设占位 key 时，Doctor 唯一配置错误是缺少 `DEEPSEEK_API_KEY`，这是预期行为。

### 15.3 最新真实组件冒烟

同一次 600519.SH 实测：

```text
AKShare market       750 根，最新 2026-08-05，close=1306.45
AKShare fundamental  贵州茅台，5 期，data_source=akshare
CNInfo retrieval     2025 年年报 PDF，披露日 2026-04-17，提取 3000 字符
Kronos               NeoQuasar/Kronos-mini，MPS，2_trading_days，概率和 1.0
Kronos source         67b630e67f6a18c9e9be918d9b4337c960db1e9a
```

注意：这是组件级 Live 验证，不包含真实 DeepSeek API，因而不等于完整 technical/fundamental Live Run。

### 15.4 测试分类

- 默认测试完全离线，不下载模型。
- `market_live`：真实 AKShare 行情。
- `fundamental_data_live`：真实公司和财务数据。
- `kronos_live`：需官方源码/模型。
- `live`：需 Pi provider/model/key。
- `research_search_live` 现有可选测试主要针对 Tavily；巨潮有独立单元测试和本轮手工 Live 冒烟。

---

## 16. 文档间的差异与冲突

| 主题 | 旧文档 | 当前事实 | 处理建议 |
| --- | --- | --- | --- |
| Pi | `MVP_ACCEPTANCE.md` 说 provider/model 未配 | 已配 `deepseek/deepseek-v4-flash`，仅缺真 key | 以本文档和代码为准，后续更新验收文档 |
| Kronos | 旧验收说未安装 | 已拉源码、缓存 mini/2k、MPS 真推理通过 | 更新 checklist |
| 检索 | 旧文档认为 Tavily 是唯一 Live | 已有默认免密钥巨潮 adapter，Tavily 可选 | 以 `README.md` 和 `app/retrieval/` 为准 |
| 行情 | README 仍主要描述 `stock_zh_a_hist` | 代码已增加新浪 `stock_zh_a_daily` 回退 | 后续补 README 精确描述 |
| Docker | README 看似可单机部署 | 容器不包含 Kronos Live 安装 | 作为下一个高优修复 |
| 架构 | 技术方案结尾说“一个 LangGraph 工作流” | 代码实际有 Runtime smoke、Technical、Fundamental 多个 graph | 以代码为准 |

---

## 17. 已知限制和技术债

### 17.1 发布阻断

1. 当前代码未正式提交/push，这是迁移的最大风险。
2. 缺真实 DeepSeek key 和完整 Live E2E。
3. Docker Live 不能运行 Kronos，容器从未实验。

### 17.2 产品/架构限制

- 只支持中国 A 股日线。
- 只有单 Worker，没有 Redis/Celery/分布式租约。
- SQLite 适合个人单机，不适合高并发。
- 无用户认证、权限、租户、配额和审批流。
- Evidence/Assumption 只在当前 run 内有效。
- Manifest 只保留当前文件，不是完整版本仓库。
- PDF 无 OCR，巨潮检索不等于全网搜索。
- MCP 只有适配边界，没有生产实现。
- DCF 是简化 5 年模型，没有完整三表联动、复杂 WACC、蒙特卡洛或大型可比库。
- Kronos 超时的原生线程不能强杀；通过单飞锁防止并发放大问题。

### 17.3 运行数据注意事项

- `data/research.db`、`data/checkpoints.db`、`data/artifacts/`、`logs/`、`backups/` 是本地运行数据，迁移时需单独决定是否保留。
- 不要把 `.env`、Hugging Face 缓存、`.venv`、`node_modules` 作为源码提交。
- `vendor/kronos` 是可根据锁文件重建的第三方源码，不应直接纳入主仓库。

---

## 18. 建议新 Agent 的接手顺序

### P0：保全当前工作区

1. 读本文档、`README.md`、产品方案、技术方案。
2. 运行 `git status --short`，识别用户修改和运行数据。
3. 不得执行 `git reset --hard`、`git clean -fd`或覆盖式 checkout。
4. 先建立经用户确认的基线 commit/push，再做大改。

### P1：完成真实 Live E2E

1. 让用户在本地 `.env` 自行填写 `DEEPSEEK_API_KEY`，不在聊天中传输密钥。
2. `python -m app.ops doctor`。
3. 停止正式 Worker，确保队列为空。
4. `python scripts/live_smoke.py --technical 600519`。
5. `python scripts/live_smoke.py --fundamental 600519`。
6. 人工检查正式报告的 Evidence、财务数字、估值假设、Kronos 解释和禁止交易指令。
7. 更新 `MVP_ACCEPTANCE.md` 和 `RELEASE_CHECKLIST.md`。

### P2：修复 Docker Live

1. 决定是否在镜像中安装 PyTorch/Kronos，还是把 Kronos 作为宿主机能力不支持容器 Live。
2. 如支持，必须在 Dockerfile 安装 `requirements-kronos.txt`、复制/锁定官方源码、设计模型缓存预热。
3. Docker Linux 不支持 Apple MPS，容器配置应使用 CPU 或明确的 GPU runtime。
4. 执行 `docker compose config/build/up`、Web/Worker 重启和数据卷验收。

### P3：后续功能

- 增加 MCP Search adapter，但保留 Protocol/Registry 和工具白名单。
- 增加更多权威来源和人工补材入口。
- 完善 PDF OCR、交易日历、多数据源对账。
- 在用户量增长时再评估 PostgreSQL、对象存储和真正任务队列，不要提前微服务化。

---

## 19. 新 Agent 必读文件顺序

1. `项目迁移交接文档_2026-08-06.md`
2. `README.md`
3. `金融投研Agent产品方案_V2.1.md`
4. `金融投研 Agent 技术方案.md`
5. `app/config.py`
6. `app/main.py`、`app/worker.py`、`app/run_service.py`、`app/models.py`
7. `app/runtime/`
8. `app/technical/workflow.py`
9. `app/fundamental/workflow.py`
10. `app/fundamental/result_manifest.py`
11. `app/retrieval/`、`app/fundamental/evidence.py`
12. `MVP_ACCEPTANCE.md`、`RELEASE_CHECKLIST.md`（作为历史快照读）

---

## 20. 当前 Codex 对话 Session

### 20.1 Session ID

```text
019fd049-7284-7160-98bc-60ae7e9e6991
```

### 20.2 本机存储文件

```text
/Users/dongyushu/.codex/sessions/2026/08/05/rollout-2026-08-05T12-58-22-019fd049-7284-7160-98bc-60ae7e9e6991.jsonl
```

Session 元数据：

```text
source: vscode
cwd: /Users/dongyushu/Downloads/vibe- coding/投研agent
Codex CLI version: 0.146.0-alpha.9.2
```

该 JSONL 是 Codex 的本地原始对话/工具调用记录，可用于审计和回溯，不应提交到项目 Git，因为它可能包含大量本机路径、工具输出和对话上下文。

---

## 21. 可直接给新 Agent 的开场语

```text
请先阅读项目根目录的《项目迁移交接文档_2026-08-06.md》、README.md、
《金融投研Agent产品方案_V2.1.md》和《金融投研 Agent 技术方案.md》。
当前工作区有大量未跟踪的已实现源码，不得执行 git clean/reset 或从远端覆盖。
先运行 git status --short、pytest -q 和 pi_bridge 的 npm test，核对交接文档中的验证状态。
未经用户授权不要 commit/push。当前首要任务是保全并建立真实代码基线，
然后配置用户本地 DeepSeek key 并完成两条 Live E2E。
```

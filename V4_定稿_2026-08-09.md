# 金融投研 Agent V4 定稿（2026-08-09）

> 执行路径：**增量 V4**（用户选定）。保持节点名/结果名/文件名/拓扑不变，在现有 `fundamental_v1` 上实现全部丰富性改进 + 写前证据核验 + 证据解耦，仅同步更新被触动的少量断言。不改权威 JSON 边界（财务/估值仍由受信 Python 计算）。

---

## 一、V4 目标与边界

### 目标（用户真正关心的价值）
1. **检索丰富性**：方向分组检索（披露/研报/Web 三固定方向）+ 类型配额，每个来源每查询只调一次，提升来源异质性，避免同类冗余。
2. **并行提速**：聚合层 intra-source fan-out 由串行 for-loop 改 ThreadPoolExecutor（~12s→~3s）。
3. **摘要优先**：search 返回 metadata+summary，Agent 先筛选，只读它要引用的正文；per-node 字符预算。
4. **预算放宽**：单节点工具调用上限 8→10（profile + config 同步），运行时 = min(profile, config)。
5. **Round 2 条件触发**：研究轮在 sub-questions 已答 + 2 类来源覆盖时早停；不足才触发补充研究。
6. **写前证据核验**：Writer 前增加确定性 Python 结构步骤，机械校验 output 中每个 evidence_id/assumption_id 在集合中存在（内存态，不加产物文件）。
7. **证据解耦**：从研究轮 DEPENDENCIES 移除 "evidence"，使 SHA 变更不再级联到中间节点；仅 Material Map/Writer/Report 绑定 evidence 做 staleness。

### 不变边界（权威/安全约束）
- **权威数字边界**：财务/估值仍由受信 Python 计算（`get_financial_data`/`calculate_financial_metrics`/`calculate_valuation`）；full agent 不得改写权威数字；`_fundamental_writer` gate 不变。
- **拓扑不变**：`NODE_ORDER` 10 节点、`RESULT_ORDER` 15 项、`FILENAMES`、17 产物文件（含 `result_manifest.json`、`fundamental_research_package.md`）全部不变。
- **`validate_references`（运行时防伪造）保留**：仍校验 output 中 evidence_id/assumption_id 在集合中存在。V4 新增的"写前核验"是同一约束在 Writer 节点入口的提前机械执行（确定性 Python），不替换 `validate_references`。
- **密钥安全**：API Key 只经环境变量注入，不进协议/DB/Artifact/日志。
- **Live 不回退 Mock**；Mock 行为不动；SSRF 黑名单（含刻意排除 198.18/15）不动。
- **Provider Protocol**：`search(*, query, symbol, max_results, timeout)` 4 kwargs 不变；`sources` 不得转发给 sub-provider（否则破坏 `_FakeProvider` 4-kwarg stub）。

---

## 二、9 阶段流程（逻辑视图）

```
resolve_security
  → lead_planning（Lead Discovery）
  → business_research + industry_research（Research Round 1）
  → lead_review（Lead Synthesis）
  → [Round 2 条件触发]（不足才补研究）
  → lead_final_review（Lead Final Synthesis / Material Map，唯一绑定 evidence 的中间节点）
  → financial_research + valuation_result + valuation_research（Python 边界，不改）
  → fundamental_writer（写前核验 + Writer）
  → write_fundamental_report（Report，确定性渲染）
```

> 物理上仍是 `fundamental_v1` 的 10 节点 `NODE_ORDER`。"9 阶段"是逻辑分组：`lead_final_review` 兼任"最终综合 / Material Map"，并在成功后调用 `generate_research_package`（既有，不变）。

### Round 2 条件触发（in-prompt，非新节点）
研究轮 task prompt 增加**早停判据**与**补研究判据**：
- **早停**：sub-questions 已回答 + 已覆盖 ≥2 类来源（announcement/research_report/news/web）→ 立即输出 `specialist_research_output`。
- **补研究（Round 2）**：若首轮检索未覆盖目标方向或核心 sub-question 未答，可再发 1 次互补主题聚合检索并读取来源；仍受 10 次预算上限约束。
- 这是对**同一节点内**多轮检索的引导，不新增 LangGraph 节点、不新增边、不破坏线性拓扑。

---

## 三、证据解耦（核心结构变更）

### 现状
`DEPENDENCIES["business_research"] = ["lead_plan", "company_profile", "evidence"]`，`DEPENDENCIES["industry_research"] = ["lead_plan", "evidence"]`。`evidence` 是 `EDITABLE_INPUTS`，SHA 变更 → 版本+1 + current + 进 `changed_sources` → 沿 DEPENDENCIES 传播 → business_research/industry_research 被标 stale → 全链重建。中间节点被 evidence 抖动裹挟。

### V4 变更
```python
# result_manifest.py
DEPENDENCIES = {
    "business_research": ["lead_plan", "company_profile"],          # 移除 "evidence"
    "industry_research": ["lead_plan"],                              # 移除 "evidence"
    "lead_review": ["business_research", "industry_research"],
    "financial_research": ["lead_review", "financial_data", "financial_metrics"],
    "valuation_result": ["financial_data", "financial_metrics", "assumptions"],
    "valuation_research": ["financial_research", "valuation_result", "assumptions"],
    "lead_final_review": ["business_research", "industry_research", "financial_data", "financial_metrics", "financial_research", "valuation_research"],
    "fundamental_writer": ["lead_plan", "business_research", "industry_research", "financial_research", "valuation_research", "lead_final_review", "evidence", "assumptions"],  # evidence 保留
    "fundamental_report": ["fundamental_writer", "financial_metrics", "valuation_result", "evidence", "assumptions"],  # evidence 保留
}
```

### 解耦后行为
- evidence.json SHA 变更 → `_audit_unlocked` 仍把 evidence 标 current + 进 `changed_sources`（EDITABLE_INPUTS 逻辑不变），但传播队列沿 DEPENDENCIES 找下游：business_research/industry_research 不再以 evidence 为依赖 → 不被传播 → **stale = ["fundamental_writer", "fundamental_report"]**，最小索引 = 8（fundamental_writer）→ **从 Writer 起重建，跳过全部中间节点**（设计目标）。
- `context_refs` 中 `artifact:evidence` 仍传给 business_research/industry_research（运行时上下文可见，Agent 仍能引用已有 evidence），仅 staleness 不再级联——这正是"读得到、不重算"。
- `_record_node_results` 中 `if node == "industry_research" and business_research.json exists: store.refresh("business_research")`：解耦后 business_research 的 inputs 不含 evidence，`refresh` 仍可调用（幂等，只刷新 SHA/输入基线），成为无害 no-op，**保留不动以最小化改动**。

### 被触动的测试断言（`tests/test_result_manifest.py`，仅 3 处）
1. **Line 15** `test_fixed_dependencies_match_phase_five_contract`：`business_research` 断言由 `["lead_plan", "company_profile", "evidence"]` → `["lead_plan", "company_profile"]`（其余两行不动）。
2. **Line 87** `test_evidence_change_propagates_to_reference_consumers`：`stale[0:2] == ["business_research", "industry_research"]` → `stale == ["fundamental_writer", "fundamental_report"]`（后续 `fundamental_writer in stale` / `fundamental_report in stale` / `lead_plan not in stale` 仍成立）。
3. **Lines 110-120** `test_record_uses_input_sha_compare_and_swap`：篡改的文件由 `evidence.json` 改为 `lead_plan.json`（business_research 仍依赖 lead_plan），使 `input_hashes("business_research")` 仍能检出变化、仍抛 `ManifestInputChangedError`。
- 其余测试（`test_unchanged_inputs_remain_current`、`test_financial_metrics_change_marks_only_downstream_stale`、`test_persisted_stale_status_cannot_self_clear`、`test_refresh_rejects_changed_result_bytes`、`test_manifest_*`）**全部存活**。

### Workflow 级安全性确认
- 无 workflow 级测试断言"evidence 变更 → business_research 重建"。
- assumption 变更重建测试存活（business_research 不依赖 assumptions）。
- 缺 evidence → human_review 测试存活（`_editable_inputs_available` 检文件存在性，与 DEPENDENCIES 无关）。

---

## 四、检索丰富性（aggregator + tools + evidence）

### aggregator.py：并行 fan-out + 类型配额 + sources 过滤
1. **ThreadPoolExecutor 并行 fan-out**：每个 provider 一个 task；完成后**按 `provider_names` 顺序重组结果**再去重，以保留 first-seen 语义（保证 `test_aggregator_dedups_same_url_across_sources` 的"from-a 因 a 先跑而胜出"成立）。per-provider 失败隔离不变（`ResearchSourceError` 与任意 `Exception` 仅记录跳过，全失败才报错）。
2. **类型配额**：在全局 `collected[:max_results]` 截断**之后**做软配额。设计为**opt-in**（默认 None → 不启用），保证 `test_aggregator_truncates_to_max_results`（5 个同类 news、max_results=3、期望 3）不被配额逻辑误减。仅在调用方显式请求配额时才按方向/类型再平衡。
3. **`sources=None` 方向过滤**：`search` 增加可选 `sources: list[str] | None = None`。**在 aggregator 内部**于调用 `provider.search` 前按 provider 名过滤（仅调用名字在 `sources` 中的子 provider）；**绝不**把 `sources=` 透传给 `provider.search`（避免破坏 4-kwarg Protocol 与 `_FakeProvider` stub）。现有 4-kwarg 调用全部保持绿色。

### fundamental_tools.py：SearchInput 增加 sources
- `SearchInput` 增加 `sources: list[str] = Field(default_factory=list)`（`extra="forbid"` 允许已定义可选字段）。
- `search_handler` 把 `args.sources` 透传给 `search_research_sources`。
- Live 下 Agent 可在 search 工具参数里指定方向（如 `["official_crawler","akshare_reports"]`），为空则 fan-out 全部（默认行为不变）。

### evidence.py：sources 只透传给 aggregator
- `search_research_sources(query, symbol, settings)` 增加可选第 4 参 `sources: list[str] | None = None`。
- Mock 分支与 research_search_mode 校验不变。
- Live 分支：若 provider 是 `AggregatingSearchProvider`（isinstance 检查，lazy import 避免循环），把 `sources` 传给 `aggregator.search`；否则（tavily/firecrawl/official_crawler/akshare_* 等单 provider）**不传 sources**（这些 provider 不接受 sources，且单 provider 无方向分组需求）。
- 现有 3-arg positional 调用全部保持绿色。

---

## 五、预算放宽（config + profiles）

- `app/config.py`：`max_tool_calls_per_node` 默认 8→10（Field default + `from_env` 默认值 "8"→"10"）。运行时 = min(profile.max_tool_calls, settings.max_tool_calls_per_node)，两者都到 10 才使 10 生效。
- `.env.example` / `.env.live.example`：`MAX_TOOL_CALLS_PER_NODE=8` → `=10`，注释 schema le=12。
- 三个 profile（`fundamental_lead`/`business_research`/`industry_research`）：`max_iterations`/`max_tool_calls` 8→10；system_prompt 同步把"工具预算上限 8 次迭代/8 次工具调用"改 10，并补**摘要优先**与**方向分组**与**Round 2 条件**措辞。
- 受限 profile（`financial_research`/`valuation_research`/`fundamental_writer`：max_tool_calls=0、无工具）**不动**。

### 工具计数不受影响
`tests/test_fundamental_workflow.py` 的 tool_call_count 断言由 `MockPiClient` 驱动（不读 prompt），prompt 4→10 措辞不改变计数：lead_planning=3、business_research=3、industry_research=2、lead_review=0、financial_research=0、valuation_research=0、lead_final_review=0、fundamental_writer=0。

---

## 六、写前证据核验（workflow.py）

### 位置
`_fundamental_writer` 在加载 `final = LeadFinalReviewOutput(...)`（line 475-477）之后、`_run_agent`（line 496）之前。仅在**重新生成分支**（非 `path.is_file() and completed`）执行；缓存命中分支已有 `validate_writer_output` 兜底。

### 实现（确定性 Python，内存态）
新增 helper `_verify_writer_input_references(directory, lead_final_review_output) -> None`：
- 加载 `evidence.json` → `EvidenceCollection`，取 `available_evidence = {item.id}`。
- 加载 `assumptions.json` → `AssumptionStore`，取 `available_assumptions = {item.id}`。
- 扫描 `lead_final_review` 上下文里 Agent 即将引用的 anchor 集合：核验**已生成的上游研究产物**中实际出现的 evidence_id/assumption_id 全部在集合内。具体地，遍历 `business_research.json`/`industry_research.json`/`financial_research.json`/`valuation_research.json`/`lead_review.json`/`lead_final_review.json` 中每个 finding 的 `evidence_ids`/`assumption_ids`，凡引用的 ID 必须存在于对应集合。
- 缺失即抛 `ValueError("写前核验失败：引用了不存在的 Evidence/Assumption ID")` → 由 `_fundamental_writer` 的 `except Exception` 捕获 → `fail_execution(... "SEMANTIC_VALIDATION_FAILED" ...)` + `_fail(... "FUNDAMENTAL_WRITER_FAILED")`，与既有失败语义一致（不新增 error_code、不新增状态、不新增产物文件）。
- 这是对 `validate_references`（运行时防伪造，仍保留）的**提前机械预检**：在花一次 Agent 调用生成 Writer 文本之前，先确认所有上游引用合法，避免 Writer 基于"悬空引用"生成后再被 `validate_writer_output` 拒掉而浪费一轮。

### 不变项
- 不新增 artifact 文件（与"写作包写前证据核验（内存态、不加产物文件）"一致）。
- 不改 `WRITER_CONTEXT_REFS`（11 项不变 → `test_fundamental_final_integration.py` 的 11-name allowlist 不破）。
- 不改 `validate_writer_output` / `validate_references`。
- 不改 `generate_research_package` / `research_package_is_current`（既有，写前已调用）。

---

## 七、实施顺序（冲突最小、可逐项验证）

1. **aggregator.py**（最隔离）：并行 fan-out + opt-in 类型配额 + `sources=None` 内部过滤。先跑 `test_aggregator_retrieval.py` 与 `test_fundamental_tools.py` 确认绿。
2. **fundamental_tools.py**：`SearchInput.sources` + 透传。
3. **evidence.py**：`sources` 第 4 参，仅透传 aggregator。
4. **result_manifest.py**：移除 2 处 evidence 依赖。
5. **test_result_manifest.py**：更新 3 处断言。
6. **config.py + .env.example + .env.live.example**：8→10。
7. **profiles**：3 个 profile 8→10 + prompt 措辞。
8. **workflow.py**：写前核验 helper + 接入 + prompt 措辞（Round 2 条件、摘要优先、方向分组）。
9. 全量 `.venv/bin/python -m pytest`（基线 316 passed, 4 skipped），逐项查逻辑冲突与 bug。
10. **不** git commit/push（V2 约束未解除）。

每步后本地跑相关测试子集，全量留到最后一次确认。

---

## 八、验收口径

- 离线：全量 `.venv/bin/python -m pytest` ≥ 316 passed, 4 skipped，无新增 fail。
- Mock 行为不变：Mock 模式离线可跑、结果不变。
- 权威边界不变：财务/估值仍由 Python 计算，Writer gate 不动。
- Live（用户本机自行配 key 后）：研究轮来源异质性提升、并行提速、evidence 抖动不再裹挟中间节点、写前发现悬空引用。
